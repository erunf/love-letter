[build]
  command = "npm run build"
  publish = "dist"

# Strip /love-letter/ prefix (proxied from games.erun.ai)
[[redirects]]
  from = "/love-letter/*"
  to = "/:splat"
  status = 200
  force = true

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
